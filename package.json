{
  "name": "contract-pdf-worker",
  "version": "1.0.0",
  "description": "Node + Playwright PDF worker for signed contract HTML-to-PDF",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "start": "node index.js",
    "build": "npx playwright install chromium",
    "install-browsers": "npx playwright install chromium"
  },
  "engines": {
    "node": "20.x"
  },
  "dependencies": {
    "express": "^4.21.0",
    "playwright": "^1.49.0"
  }
}
